name: Build and Publish Dev Container Images
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 0'
env:
  REGISTRY: ghcr.io
  BASE_IMAGE_NAME: base
  BASE_IMAGE_PATH: psi-polska/devcontainers/base
  CD_IMAGE_NAME: cloud-developer
  CD_IMAGE_PATH: psi-polska/devcontainers/cloud-developer
  JAVA_IMAGE_NAME: java-developer
  JAVA_IMAGE_PATH: psi-polska/devcontainers/java-developer

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest
    permissions:
      artifact-metadata: write
      attestations: write
      contents: write
      id-token: write
      packages: write
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to ${{ env.REGISTRY }}
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: (BASE) Extract metadata
        id: meta-base
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.CD_IMAGE_PATH }}

      - name: (BASE) Build and push
        id: build-base
        uses: docker/build-push-action@v7
        with:
          context: src/${{ env.BASE_IMAGE_NAME }}
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ env.REGISTRY }}/${{ env.BASE_IMAGE_PATH }}
          labels: ${{ steps.meta-base.outputs.labels }}
          no-cache: true

      - name: (BASE) Generate artifact attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.BASE_IMAGE_PATH}}
          subject-digest: ${{ steps.build-base.outputs.digest }}
          push-to-registry: ${{ github.ref == 'refs/heads/main' }}

      - name: (CD) Extract metadata
        id: meta-cd
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.CD_IMAGE_PATH }}

      - name: (CD) Build and push
        id: build-cd
        uses: docker/build-push-action@v7
        with:
          context: src/${{ env.CD_IMAGE_NAME }}
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ env.REGISTRY }}/${{ env.CD_IMAGE_PATH }}
          labels: ${{ steps.meta-cd.outputs.labels }}
          no-cache: true

      - name: (CD) Generate artifact attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.CD_IMAGE_PATH}}
          subject-digest: ${{ steps.build-cd.outputs.digest }}
          push-to-registry: ${{ github.ref == 'refs/heads/main' }}

      - name: (JAVA) Extract metadata
        id: meta-java
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH }}

      - name: (JAVA) Build and push
        id: build-java
        uses: docker/build-push-action@v7
        with:
          context: src/${{ env.JAVA_IMAGE_NAME }}
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH }}
          labels: ${{ steps.meta-java.outputs.labels }}
          no-cache: true

      - name: (JAVA) Run Trivy scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH }}
          format: 'sarif'
          scanners: 'vuln'
          severity: 'CRITICAL,HIGH'
          limit-severities-for-sarif: true
          output: 'trivy-java.sarif'

      - name: (JAVA) Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-java.sarif'

      - name: (JAVA) Generate SBOM (github) and submit to Dependency Graph
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH }}
          format: 'github'
          output: 'sbom-github-java_developer.json'
          github-pat: ${{ secrets.GITHUB_TOKEN }}

      - name: (JAVA) Generate SBOM (CycloneDX)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH }}
          format: 'cyclonedx'
          output: 'sbom-cyclonedx-java_developer.json'

      - name: (JAVA) Upload SBOM (CycloneDX) as a Github artifact
        uses: actions/upload-artifact@v7
        with:
          name: trivy-sbom-report
          path: 'sbom-cyclonedx-java_developer.json'

      - name: (JAVA) Generate artifact attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.JAVA_IMAGE_PATH}}
          subject-digest: ${{ steps.build-java.outputs.digest }}
          push-to-registry: ${{ github.ref == 'refs/heads/main' }}
